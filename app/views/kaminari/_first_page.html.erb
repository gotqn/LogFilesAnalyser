<li>
  <% if current_page.first? %>
    <%= link_to raw(t 'views.pagination.first'), url, remote: remote, class: 'btn disabled' %>
  <% else %>
    <%= link_to raw(t 'views.pagination.first'), url, remote: remote %>
  <% end %>
</li>
