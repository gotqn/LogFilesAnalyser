<li>
  <% if current_page.first? %>
      <%= link_to raw(t 'views.pagination.previous'), url, remote: remote, rel: 'prev', class: 'btn disabled' %>
  <% else %>
      <%= link_to raw(t 'views.pagination.previous'), url, remote: remote, rel: 'prev' %>
  <% end %>
</li>
