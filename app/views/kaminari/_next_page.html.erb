<li>
  <% if current_page.last? %>
      <%= link_to raw(t 'views.pagination.next'), url, remote: remote, rel: 'next', class: 'btn disabled' %>
  <% else %>
      <%= link_to raw(t 'views.pagination.next'), url, remote: remote,  rel: 'next' %>
  <% end %>
</li>
