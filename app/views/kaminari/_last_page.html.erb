<li>
  <% if current_page.last? %>
      <%= link_to raw(t 'views.pagination.last'), url, remote: remote, class: 'btn disabled' %>
  <% else %>
      <%= link_to raw(t 'views.pagination.last'), url, remote: remote %>
  <% end %>
</li>
